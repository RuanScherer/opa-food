import React from 'react'
import { ButtonBox, ButtonLabel } from './styles'

const BottomTabButton = ({ label }) => (
  <ButtonBox>
    <ButtonLabel>{label}</ButtonLabel>
  </ButtonBox>
)

export default BottomTabButton